# Offline source mirror

This directory contains the source assets and selected Vue/component files needed for the ongoing native Swift migration.

- Original source: `/opt/friend/uni-app`
- Local mirror: `SourceReference/uni-app`
- Native app code: `Friend/`
- The mirror is reference material only; it is excluded from the Xcode build phase.
- Keep this mirror until all required pages, assets, and behavior have been migrated and verified in the native app.
